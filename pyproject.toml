[project]
name = "fga-preview-build"
version = "0.1.0"
description = "Documentation site for the FGA Preview build project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
	"zensical==0.0.38",
]

[tool.uv]
package = false
required-version = "==0.11.8"
